# =============================================================================
# backend/app/schemas/__init__.py
# Package marker for DMRE Pydantic request/response schemas.
# Schemas are kept separate from ORM models so the API contract is explicit
# and changes to the DB layer don't accidentally leak to the HTTP interface.
# =============================================================================

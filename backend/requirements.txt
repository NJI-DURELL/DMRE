# =============================================================================
# DMRE Backend Python Dependencies
# Install with: pip install -r requirements.txt
# All versions pinned for reproducibility.
# =============================================================================

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # Required for file upload endpoints (voice/image)

# --- Database (async) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0                  # Async PostgreSQL driver used by SQLAlchemy and Alembic
alembic==1.13.1

# --- Settings & environment ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- AI: Sentence-BERT embeddings ---
sentence-transformers==2.7.0     # Loads all-MiniLM-L6-v2; pulls torch transitively

# --- AI: Speech-to-text ---
# faster-whisper is used instead of openai-whisper: identical accuracy,
# Python 3.13 compatible, and 4x faster on CPU.
faster-whisper==1.0.3

# --- AI: OCR ---
pytesseract==0.3.10
Pillow==11.0.0

# --- Vector store ---
chromadb==0.6.3

# --- Re-ranker (loads pre-trained reranker_model.pkl) ---
# Versions bumped to Python 3.13-compatible releases.
# XGBoost 2.x models saved with 2.0.x load fine under 2.1.x.
xgboost==2.1.3
scikit-learn==1.5.2
numpy==2.1.0
pandas==2.2.3

# --- Blockchain ---
web3==6.18.0

# --- Utilities ---
httpx==0.27.0                    # Async HTTP client for internal calls if needed
python-jose[cryptography]==3.3.0 # JWT handling (reserved for auth extension)

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0                    # Also used as ASGI test client via AsyncClient

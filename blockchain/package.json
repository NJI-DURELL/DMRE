{
  "name": "dmre-blockchain",
  "version": "1.0.0",
  "description": "DMRE blockchain integrity layer — Hardhat + Solidity",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "solc": "^0.8.24"
  }
}
